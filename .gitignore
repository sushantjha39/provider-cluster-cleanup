node_modules/
.env
config.yaml
logs/
.kubeconfigs/
*.log
.DS_Store

# Claude Code records the exact commands it has been allowed to run, which can
# include tokens and passwords typed into curl. These files are rewritten by the
# tool itself, so scrubbing them by hand does not stick — never track them.
.claude/settings.json
.claude/settings.local.json
